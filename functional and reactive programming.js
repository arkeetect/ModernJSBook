// functional and reactive programming

// declarative - pure function, avoid side effects and avoid mutating shared state

// stream of events

// functionally iterating over array (without knowledge of the input data or mutating the passed in data)

// forEach (no need to know index)

// Array.map()  - it returns a transformed data

// RxJS for reactive extensions - e.g. observer pattern

